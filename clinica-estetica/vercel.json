{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed.ts && npx next build",
  "outputDirectory": ".next",
  "env": {
    "DATABASE_URL": "@database_url",
    "AUTH_SECRET": "@auth_secret",
    "ANTHROPIC_API_KEY": "@anthropic_api_key"
  }
}
